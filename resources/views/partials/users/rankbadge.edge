@if(user.rank === 'Banned')
  <div class="badge badge-banned">{{ user.rank }}</div>
@elseif(user.rank === 'User')
  <div class="badge">{{ user.rank }}</div>
@elseif(user.rank === 'Moderator')
  <div class="badge badge-mod">{{ user.rank }}</div>
@elseif(user.rank === 'Admin')
  <div class="badge badge-admin">{{ user.rank }}</div>
@else
  <div class="badge badge-superadmin">{{ user.rank }}</div>
@endif